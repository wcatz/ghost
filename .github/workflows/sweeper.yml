# Closes the review loop after the Reviewer workflow runs.
#
# Validated mechanics (wcatz/ci-mech-spike, 2026-08-25):
#   - GITHUB_TOKEN cannot resolve review threads ("not accessible by
#     integration") and cannot submit APPROVED reviews. Resolutions go
#     through a GitHub App installation token (GH_APP_ID +
#     GH_APP_PRIVATE_KEY secrets, Pull requests: read+write on this repo
#     only). Without those secrets this job degrades to report-only.
#   - Outdated unresolved threads still block merge under required
#     conversation resolution, so stale agent findings must be swept
#     once a fresh review has re-judged the new diff.
#   - GITHUB_TOKEN CAN submit REQUEST_CHANGES reviews. Signal-only:
#     approvals stay out of branch protection entirely.
#
# Triggered by workflow_run after each Reviewer workflow completion and by
# PR comments for manual re-runs. workflow_run executes in default-branch
# context — fine here, because nothing in this file is (or may become)
# a required status check: those attach to the wrong SHA.

name: Sweeper

on:
  workflow_run:
    workflows: ["Reviewer"]
    types: [completed]
  issue_comment:
    types: [created]

permissions:
  pull-requests: write

concurrency:
  group: sweeper-${{ github.event.workflow_run.pull_requests[0].number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  loop:
    # conclusion must be 'success': types:[completed] also fires on
    # failure, cancelled and startup_failure. The sweep's whole premise
    # (see the isOutdated note below) is that a fresh review has just
    # re-judged the new diff — after a failed or cancelled review there
    # is no such judgement, and resolving stale threads then would clear
    # the merge gate on the strength of a review that never ran. Failing
    # closed leaves the threads blocking, which is the safe direction.
    if: >-
      (github.event_name == 'workflow_run' &&
        github.event.workflow_run.conclusion == 'success') ||
      (github.event_name == 'issue_comment' &&
        github.event.issue.pull_request != null &&
        github.event.sender.type != 'Bot')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      HAS_APP: ${{ secrets.GH_APP_ID != '' && secrets.GH_APP_PRIVATE_KEY != '' }}
    steps:
      - name: Resolve PR number
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" = "issue_comment" ]; then
            echo "number=${{ github.event.issue.number }}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          NUMBER="$(jq -r '.workflow_run.pull_requests[0].number // empty' "$GITHUB_EVENT_PATH")"
          if [ -z "$NUMBER" ]; then
            HEAD_SHA="$(jq -r '.workflow_run.head_sha' "$GITHUB_EVENT_PATH")"
            NUMBER="$(gh api "repos/$GITHUB_REPOSITORY/commits/$HEAD_SHA/pulls" --jq '.[0].number // empty')"
          fi
          if [ -z "$NUMBER" ] || [ "$NUMBER" = "null" ]; then
            echo "No PR associated with this run; nothing to do."
            exit 0
          fi
          echo "number=$NUMBER" >> "$GITHUB_OUTPUT"

      - name: Mint app token
        id: app-token
        if: env.HAS_APP == 'true'
        # Pinned SHA (not a mutable tag) — same supply-chain convention
        # as every other action pin here. This action mints a token carrying
        # whatever permissions the review-sweeper App has, so it is the
        # highest-value pin in this pipeline.
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          app-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}

      - name: Sweep stale findings, signal on live ones
        env:
          GH_TOKEN: ${{ github.token }}
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          [ -n "$NUMBER" ] || { echo "no PR"; exit 0; }

          # Cursor-paginated: first:100 alone silently skipped threads past
          # 100, letting stale findings survive the sweep and keep blocking
          # merge via required conversation resolution until a human
          # resolved them. The page cap bounds a pathological repo
          # (20 pages x 100 threads) so the loop can never run away.
          # shellcheck disable=SC2016
          HEADREF="$(gh api graphql \
            -f query='query($owner:String!,$name:String!,$number:Int!){repository(owner:$owner,name:$name){pullRequest(number:$number){headRefOid}}}' \
            -f owner="${GITHUB_REPOSITORY%%/*}" \
            -f name="${GITHUB_REPOSITORY##*/}" \
            -F number="$NUMBER" \
            --jq '.data.repository.pullRequest.headRefOid')"

          PAGES_FILE="$(mktemp)"
          trap 'rm -f "$PAGES_FILE"' EXIT
          CURSOR_ARG=()
          for _ in $(seq 1 20); do
            # shellcheck disable=SC2016
            PAGE="$(gh api graphql \
              -f query='query($owner:String!,$name:String!,$number:Int!,$cursor:String){
                repository(owner:$owner,name:$name){
                  pullRequest(number:$number){
                    reviewThreads(first:100,after:$cursor){
                      nodes{
                        id isResolved isOutdated
                        comments(first:1){nodes{author{login}}}
                      }
                      pageInfo{hasNextPage endCursor}
                    }
                  }
                }}' \
              -f owner="${GITHUB_REPOSITORY%%/*}" \
              -f name="${GITHUB_REPOSITORY##*/}" \
              -F number="$NUMBER" "${CURSOR_ARG[@]}" \
              --jq '.data.repository.pullRequest.reviewThreads')"
            echo "$PAGE" >> "$PAGES_FILE"
            [ "$(jq -r '.pageInfo.hasNextPage' <<<"$PAGE")" != "true" ] && break
            CURSOR_ARG=(-f cursor="$(jq -r '.pageInfo.endCursor' <<<"$PAGE")")
          done

          THREADS="$(jq -n --arg head "$HEADREF" \
            '{headRefOid: $head,
              reviewThreads: {nodes: [inputs | .nodes[]]}}' \
            "$PAGES_FILE" 2>/dev/null || echo '{"headRefOid":"","reviewThreads":{"nodes":[]}}')"

          # isOutdated is a proxy, not a verdict: a thread goes outdated
          # when the diff line it was anchored to changes or disappears,
          # which can happen while the finding itself still applies. The
          # sweep trusts the fresh review that just completed to have
          # re-flagged anything still relevant on the new diff — the
          # accepted trade-off for never letting a dead thread block merge.
          STALE="$(echo "$THREADS" | jq '[.reviewThreads.nodes[]
            | select(.isResolved==false and .isOutdated==true)
            | select((.comments.nodes[0].author.login // "")
              | startswith("github-actions") or startswith("review-sweeper")) | .id]')"

          LIVE="$(echo "$THREADS" | jq '[.reviewThreads.nodes[]
            | select(.isResolved==false and .isOutdated==false)
            | select((.comments.nodes[0].author.login // "")
              | startswith("github-actions") or startswith("review-sweeper"))]')"

          N_STALE="$(jq length <<<"$STALE")"
          N_LIVE="$(jq length <<<"$LIVE")"

          if [ "$N_STALE" -gt 0 ]; then
            if [ -n "$APP_TOKEN" ]; then
              RESOLVED=0
              RESOLVE_ERR="$(mktemp)"
              for id in $(jq -r .[] <<<"$STALE"); do
                # shellcheck disable=SC2016
                if GH_TOKEN="$APP_TOKEN" gh api graphql \
                     -f query='mutation($id:ID!){resolveReviewThread(input:{threadId:$id}){thread{isResolved}}}' \
                     -f id="$id" --jq '.data.resolveReviewThread.thread.isResolved' \
                     >/dev/null 2>>"$RESOLVE_ERR"; then
                  RESOLVED=$((RESOLVED+1))
                fi
              done
              echo "Auto-resolved $RESOLVED/$N_STALE stale finding(s) (superseded by newer review)."
              # A partial or total failure used to be reported only as a
              # ratio: run 34875887374 printed "Auto-resolved 0/1" and gave
              # no way to tell a permissions problem from a stale thread id.
              # The threads stay unresolved and keep blocking merge, so this
              # is the opposite of a silent-success case — say why.
              # gh's error text is prefixed so it cannot forge a ::command.
              if [ "$RESOLVED" -lt "$N_STALE" ]; then
                echo "::warning::$((N_STALE - RESOLVED)) stale thread(s) could not be resolved and will keep blocking merge — check that the App grant is Pull requests: read & write AND that the installation has accepted it"
                sed -n '1,5p' "$RESOLVE_ERR" | sed 's/^/> /'
              fi
              rm -f "$RESOLVE_ERR"
            else
              echo "::warning::$N_STALE stale finding(s) await resolution — configure the GH_APP_ID / GH_APP_PRIVATE_KEY secrets to enable auto-resolve."
            fi
          fi

          if [ "$N_LIVE" -gt 0 ]; then
            gh api "repos/$GITHUB_REPOSITORY/pulls/$NUMBER/reviews" \
              -f event=REQUEST_CHANGES \
              -f body="The reviewer has $N_LIVE unresolved finding(s) — see inline threads." >/dev/null 2>&1 \
              && echo "Requested changes: $N_LIVE live finding(s)." \
              || echo "Could not set REQUEST_CHANGES state."
          else
            echo "No live reviewer findings."
          fi
